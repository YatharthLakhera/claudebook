<!-- claudebook: generated {{DATE}}, depth={{DEPTH}}, sha={{COMMIT_SHA_SHORT}} -->
# Tech Stack

## Snapshot

| Layer | Choice | Version |
|---|---|---|
{{STACK_TABLE}}

<!--
  Rows for: Language, Framework, Build tool, Package manager, Test runner,
  Linter, Formatter, Styling, State, Routing, HTTP, ORM/DB, Analytics.
  Skip rows that don't apply.
-->

## Constraints these choices impose

{{CONSTRAINTS}}

<!--
  Tie each constraint to a rule in conventions.md or a pattern doc.
  Example:
  - axios is the only HTTP client — `fetch()` is forbidden so the auth/error
    interceptor in src/utils/api.ts always runs. See patterns/api-integration.md.
  - TailwindCSS only — custom CSS files are not allowed. See patterns/styling.md.
  - React 18 batching means setState calls in async callbacks DO batch (different
    from React 17). See best-practices.md.
-->

## Notable dependencies

{{DEPS}}

<!--
  One line per dep that materially affects code patterns. Skip trivial deps.
  Always record the exact pinned version. Flag any dep with low adoption.
  Example:
  - axios 1.7.2 — centralized instance in src/utils/api.ts
  - lucide-react 0.522.0 — icon library; do not introduce others
  - react-toastify 10.0.6 — toast UX (top-right, 3s default)
  - mixpanel-browser 2.73.0 — analytics; events tracked via src/utils/analytics.ts
  - some-obscure-lib 0.2.1 — ⚠ Low adoption — verify credibility before use
-->

## Versions to watch

{{VERSION_NOTES}}

<!--
  Any pin justifications, known incompat, or upcoming upgrades.
  Also note: all deps must be pinned to exact versions (no ranges/carets/tildes).
  Never recommend a version released within the last 30 days.
  Skip if nothing material.
-->
