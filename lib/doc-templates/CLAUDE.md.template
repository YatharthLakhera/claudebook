<!-- claudebook: generated {{DATE}}, depth={{DEPTH}}, sha={{COMMIT_SHA_SHORT}} -->
# CLAUDE.md

> Router file. Always loaded into Claude's context. Keep ≤200 lines.
> Detailed docs live in `.claude/docs/` and are loaded only when relevant.

## Project

**{{PROJECT_NAME}}** — {{ONE_LINE_DESCRIPTION}}

## Stack

{{STACK_SNAPSHOT}}

<!--
  Example:
  - Language: TypeScript 5.x
  - Framework: React 18.3 (SPA via Vite)
  - Styling: TailwindCSS 3.4
  - Routing: react-router-dom 6
  - HTTP: axios (centralized in src/utils/api.ts)
  - State: React Context (no Redux)
-->

## Commands

| What | Command |
|---|---|
{{COMMAND_TABLE}}

<!--
  Pulled from package.json scripts (or stack equivalent).
  Include only what's actually run day-to-day.
-->

## Critical rules

{{CRITICAL_RULES}}

<!--
  5–10 must-follow rules from .claude/docs/conventions.md.
  Each one line. The full set with rationale lives in conventions.md.
-->

## Before writing ANY code

1. Check the relevant inventory to avoid recreating existing code:
{{INVENTORY_LINKS}}
2. Match the task below and load the matching pattern/topic doc.

<!--
  {{INVENTORY_LINKS}} renders ONLY the inventories that were actually generated.
  Do not include rows for skipped inventories. Examples:

    React app with all inventories:
      - Components → `.claude/docs/inventories/component-map.md`
      - Utilities  → `.claude/docs/inventories/utility-map.md`
      - Hooks      → `.claude/docs/inventories/hook-map.md`
      - Services   → `.claude/docs/inventories/service-map.md`
      - Routes     → `.claude/docs/inventories/route-map.md`
      - Types      → `.claude/docs/inventories/type-map.md`

    Python FastAPI backend (no Components, no Hooks):
      - Utilities → `.claude/docs/inventories/utility-map.md`
      - Services  → `.claude/docs/inventories/service-map.md`
      - Routes    → `.claude/docs/inventories/route-map.md`
      - Types     → `.claude/docs/inventories/type-map.md`

    Tiny project with only utilities:
      - Utilities → `.claude/docs/inventories/utility-map.md`

  If exactly zero inventories were generated, replace the bullet list with:
      (no inventories generated for this project — codebase too small)
-->


## Task → Doc routing

| If the task involves... | Load |
|---|---|
{{ROUTING_TABLE}}

<!--
  Only include rows for docs that were actually generated.
  Examples (drop the ones not generated):
  | API calls / endpoints / HTTP errors | .claude/docs/patterns/api-integration.md |
  | Modal / popup / dialog              | .claude/docs/patterns/modals.md           |
  | Form / validation / submission      | .claude/docs/patterns/forms.md            |
  | Table / list / pagination           | .claude/docs/patterns/tables.md           |
  | Styling / theme / colors            | .claude/docs/patterns/styling.md          |
  | Context / store / global state      | .claude/docs/patterns/state-management.md |
  | New route / navigation              | .claude/docs/patterns/routing.md          |
  | Architecture decisions / module map | .claude/docs/architecture.md              |
  | Stack-specific best practice check  | .claude/docs/best-practices.md            |
  | Naming / file org / tooling rules   | .claude/docs/conventions.md               |
  | Project purpose / business context  | .claude/docs/overview.md                  |
-->

## Maintenance

This documentation is generated and maintained by the [`claudebook`](https://github.com/) plugin.

- Run `/claudebook:revise` after merging significant changes.
- Last write: {{DATE}}
- Last commit covered: {{COMMIT_SHA}}

<!--
  CLAUDEBOOK.md is the source of truth for `Last commit covered`. This footer mirrors
  it for human readability. `/claudebook:revise` updates BOTH the meta file and this
  footer to the new HEAD SHA on every successful run. The top-of-file marker
  (sha=<short-sha>) is also updated when the router itself is patched.
-->

